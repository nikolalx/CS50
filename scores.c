#include <stdio.h>
#include <cs50.h>
#include <math.h>

int main(void)
{
    int scores[3];

    for(int i = 0; i < 0; i++)
    {
        scores[i] = get_int("Score ");
    }

    printf("Average %f\n", (scores[i] + scores[i] + scores[i]) / (float) i);
}